{% extends 'LayoutBundle::layout.html.twig' %}

{% block menu %}
{% endblock %}

{% block page_content -%}
    <h1><i class="icon edit"></i>Editxt</h1>
    {% for type, messages in app.session.flashBag.all %}
        {% for message in messages %}
            <div class="{{ type }}">
                {{ message|trans({}, 'FOSUserBundle') }}
            </div>
        {% endfor %}
    {% endfor %}

    {% block fos_user_content %}
    {% endblock %}

{% endblock %}
